<table width = 800 align = CENTER>
  <tr align = right>
    <td></td>    <td>
      <% form_tag ({:action => 'login', :controller => 'user'}) do %>
      <label for="user_email">Email: </label>
	<%= text_field 'user', 'email', {:size => 15} %>
	<label for="user_email">Password: </label>
	<%= password_field 'user', 'password', {:size => 15} %>
	<%= submit_tag "Login" %>
	<% end %>
    </td>
  </tr>
  <tr>
    <td><img src = "images/homepage.jpg">
    </td>
    <td align = right>
      <h4>New User ? Sign Up</h4>
      <%= form_tag :action => 'create', :controller => 'user' %>
      <%= render :partial => 'user/form' %> 
      <tr><td><%= submit_tag "Create" %></td></tr>
      </table>

    </td>
  </tr>
</table>
